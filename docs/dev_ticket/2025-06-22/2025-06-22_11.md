# [新機能] ブログ記事管理API実装

**チケットID**: `2025-06-22_11`  
**作成日**: 2025-06-22  
**担当者**: Claude  
**ステータス**: TODO

## 概要

utilアプリのブログ機能として、記事管理のためのAPI実装を行う。記事のCRUD操作、ステータス管理、タグ機能を含む。

## 背景・目的

- utilアプリの一機能としてブログ機能を追加
- 既存のClean Architecture構造を活用
- 記事の作成・編集・削除・一覧表示をAPI経由で提供

## 要件・仕様

### Phase 1: データベース設計・マイグレーション
- [x] articles テーブル作成
- [x] インデックス設定
- [x] マイグレーションファイル作成

### Phase 2: ドメイン層実装
- [x] Article エンティティ作成
- [x] ArticleRepository インターフェース定義
- [x] 記事ステータス管理（draft, published, archived）

### Phase 3: アプリケーション層実装
- [x] CreateArticleUseCase実装
- [x] GetArticlesUseCase実装（一覧取得、フィルタリング）
- [x] GetArticleUseCase実装（詳細取得）
- [x] UpdateArticleUseCase実装
- [x] DeleteArticleUseCase実装
- [x] PublishArticleUseCase実装
- [x] UnpublishArticleUseCase実装

### Phase 4: インフラ層実装
- [x] PostgresArticleRepository実装
- [x] SQL クエリ実装（CRUD操作）
- [x] タグ検索機能（JSONB）

### Phase 5: インターフェース層実装
- [ ] ArticleHandler実装
- [ ] ArticleDTO定義
- [ ] バリデーション実装
- [ ] ルーター設定

### Phase 6: API エンドポイント
- [ ] `GET /api/v1/articles` - 記事一覧
- [ ] `POST /api/v1/articles` - 記事作成
- [ ] `GET /api/v1/articles/:id` - 記事詳細
- [ ] `PUT /api/v1/articles/:id` - 記事更新
- [ ] `DELETE /api/v1/articles/:id` - 記事削除
- [ ] `POST /api/v1/articles/:id/publish` - 記事公開
- [ ] `POST /api/v1/articles/:id/unpublish` - 記事非公開

## 受け入れ条件（Definition of Done）

- [ ] 全APIエンドポイントが正常動作している
- [ ] 記事のCRUD操作が可能
- [ ] ステータス管理が正しく動作
- [ ] タグでのフィルタリングが可能
- [ ] ページネーションが実装されている
- [ ] バリデーションエラーが適切に処理される
- [ ] 単体テストが完了している
- [ ] 結合テストが完了している
- [ ] APIドキュメントが作成されている
- [ ] **開発日誌が作成されている**

## テスト要件

### 単体テスト
- [ ] Article エンティティのテスト
- [ ] 各UseCase のテスト
- [ ] Repository のテスト
- [ ] Handler のテスト

### 結合テスト
- [ ] API エンドポイントの動作テスト
- [ ] データベース連携テスト
- [ ] エラーハンドリングテスト

### テストデータ・環境
- **テスト環境**: Docker Compose
- **テストデータ**: サンプル記事データ
- **テストツール**: Go標準testing、curl

## 技術詳細

### 対象システム・モジュール
- `/internal/domain/entity/` - Article エンティティ
- `/internal/domain/repository/` - ArticleRepository
- `/internal/application/usecase/` - 記事関連UseCase
- `/internal/infrastructure/persistence/` - PostgresArticleRepository
- `/internal/interfaces/` - ArticleHandler, DTO

### 実装方針
1. **既存構造の活用**: User管理と同様のClean Architecture
2. **段階的実装**: Phase毎に実装・テスト
3. **再利用性**: 既存のミドルウェア・バリデーションを活用

### 技術スタック
- **言語・フレームワーク**: Go (Gin), PostgreSQL
- **ライブラリ**: 既存のvalidator, GORM等
- **データベース**: PostgreSQL (JSONB for tags)

### データモデル
```go
type Article struct {
    ID          string     `json:"id"`
    Title       string     `json:"title"`
    Content     string     `json:"content"`
    Summary     string     `json:"summary"`
    Status      string     `json:"status"` // draft, published, archived
    AuthorID    string     `json:"author_id"`
    Tags        []string   `json:"tags"`
    CreatedAt   time.Time  `json:"created_at"`
    UpdatedAt   time.Time  `json:"updated_at"`
    PublishedAt *time.Time `json:"published_at"`
}
```

## 関連情報

### 関連チケット
- **依存チケット**: 2025-06-22_10 (ブログシステムへの転用)
- **関連チケット**: 2025-06-22_1〜9 (基盤システム)

### API設計
- RESTful API設計
- JSON形式でのデータ交換
- 統一されたエラーレスポンス形式

### DB設計
```sql
CREATE TABLE articles (
    id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
    title VARCHAR(500) NOT NULL,
    content TEXT NOT NULL,
    summary VARCHAR(1000),
    status VARCHAR(20) NOT NULL DEFAULT 'draft',
    author_id VARCHAR(36) REFERENCES users(id),
    tags JSONB DEFAULT '[]',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    published_at TIMESTAMP WITH TIME ZONE
);
```

## リスク・課題

- **技術的リスク**: JSONB タグ検索のパフォーマンス
- **スケジュールリスク**: 既存システムとの統合複雑さ
- **依存関係**: 認証システムとの連携

## 作業見積もり

- **見積もり工数**: 1-2日
- **実績工数**: (完了時に記入)
- **優先度**: 高
- **難易度**: 中

## 進捗メモ

### 2025-06-22
- チケット作成
- 設計書作成完了

## 備考

既存のUser管理システムの構造を参考に、同様のパターンで記事管理機能を実装する。認証システムは保持し、author_idでユーザーと記事を関連付ける。