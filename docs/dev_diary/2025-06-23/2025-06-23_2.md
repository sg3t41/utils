# 開発日誌 - #2025-06-23_2

## 2025-06-23 18:06

**対応チケット**: リファクタリングプロジェクト  
**作業内容**: 
### バックエンド精査とリファクタリング

1. **重複コードの修正**
   - UserResponse構造体の重複を解消
   - `internal/interfaces/dto/user_dto.go` を新規作成し、UserDTOを統一化
   - `ToUserResponse` 関数を作成し、変換処理を統一
   - `user_handler.go` の重複したUserResponse定義を削除し、dto.ToUserResponse()を使用するよう修正

2. **ハードコーディングの修正**  
   - DefaultAuthorIDをconfigから取得するよう修正
   - `pkg/config/config.go` にDefaultAuthorIDフィールドを追加
   - `article_handler.go` でConfigを注入し、ハードコードされたauthorIDを修正

3. **エラーハンドリング統一化**
   - `internal/domain/errors/errors.go` を新規作成
   - DomainError構造体とエラータイプの定義を追加
   - `internal/interfaces/handler/error_handler.go` を新規作成
   - HandleError関数とhandleDomainError関数を実装

4. **セキュリティ問題の修正**
   - `upload_handler.go` のパストラバーサル攻撃対策を強化
   - 絶対パス解決による範囲外アクセス防止機能を追加
   - ファイルパスの検証を強化

### フロントエンド精査とリファクタリング

1. **型定義の統一化**
   - `types/article.ts` を新規作成
   - Article, ArticleListResponse, CreateArticleRequestなどの型定義を統一
   - 重複していた型定義を削除

2. **ユーティリティ関数の統一化**
   - `utils/dateFormat.ts` を新規作成
   - formatDate, formatRelativeDateを共通化
   - `utils/statusUtils.ts` を新規作成  
   - getStatusBadge, getStatusText, statusOptionsを共通化

3. **API クライアントの拡張**
   - `lib/api.ts` にArticleApiClientクラスを追加
   - 記事関連のAPI呼び出しを統一化
   - 画像アップロード機能とURL生成機能を追加

### 発見された主な問題点

1. **バックエンド**
   - UserResponse構造体の重複定義（4箇所）
   - ハードコードされたDefaultAuthorID
   - 統一されていないエラーハンドリング
   - パストラバーサル脆弱性の可能性

2. **フロントエンド**  
   - Article型定義の重複（3箇所）
   - 日付フォーマット関数の重複
   - ステータス関連処理の重複
   - API URLのハードコーディング

**テスト結果**:
- ビルドエラーの修正: ✅ 成功
- Docker環境での動作確認: ✅ 成功
  - API ヘルスチェック: ✅ OK
  - 記事一覧取得: ✅ OK  
  - 検索機能（特殊文字対応）: ✅ OK
  - フロントエンド表示: ✅ OK

**次回の予定**:
- 残存する記事関連ページの統一API使用への移行
- エラーハンドリングのさらなる改善
- セキュリティ強化（認証・CSRF対策）

**学習・知見**: 
- Go言語でのDTOパターンとDependency Injectionの活用
- Next.js 15でのTypeScript型安全性の重要性
- Docker環境でのAPI/フロントエンド連携テスト方法
- リファクタリング時のビルドエラー解決プロセス

**愚痴・雑感**: 
大規模リファクタリングは予想以上に時間がかかるが、コードの重複削除と型安全性の向上により、将来的なメンテナンス性が大幅に向上した。特にバックエンドのUserResponse重複問題の解決は、ビルドエラーの温床を解消できて満足。Docker環境でのテストもスムーズに動作し、本番環境デプロイへの準備も整った感触。フロントエンド側の型統一により、開発者体験も向上すると期待される。