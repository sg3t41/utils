# 開発日誌 - #2025-06-23_1

## 2025-06-23 17:45

**対応チケット**: `#2025-06-22_11, #2025-06-22_12, #2025-06-22_13`

**作業内容**:
- チケット #2025-06-22_11 の完了作業（テスト・ドキュメント作成）
- チケット #2025-06-22_12 の状況確認と完了処理
- チケット #2025-06-22_13 の検索機能バグ調査と解決

### チケット #2025-06-22_11 完了作業

1. **記事関連の単体テストを作成**:
   - `internal/application/usecase/article_usecase_test.go` を新規作成
   - MockArticleRepository の実装
   - CreateArticleUseCase と GetArticleUseCase のテスト実装
   - 正常系とエラー系のテストケースを網羅

2. **結合テスト実施**:
   - 記事検索API (`GET /articles?search=React`) の動作確認
   - 記事作成API (`POST /articles`) の動作確認
   - 全てのAPIエンドポイントが正常に動作することを確認

3. **APIドキュメント作成**:
   - `api/doc/article_api.md` を新規作成
   - 全エンドポイントの仕様を詳細に記載
   - リクエスト/レスポンス例、エラーレスポンス、データ型定義を含む

### チケット #2025-06-22_12 状況確認

**発見事項**: 実装が既に完了していた
- Phase 1-4の全項目が実装済み
- 記事一覧ページ（検索、フィルタ、ページネーション機能付き）
- 記事詳細ページ（公開/非公開切り替え、編集/削除機能付き）
- 記事作成・編集フォーム（画像アップロード機能付き）
- レスポンシブデザイン、ローディング状態、エラーハンドリング

### チケット #2025-06-22_13 バグ調査と解決

**問題**: 検索で特殊文字を使った際に全記事が返ってくる

**原因の特定**:
- テストで `keyword` パラメータを使用していたが、実際のAPIは `search` パラメータを使用
- `keyword` パラメータは無視されるため、フィルタ条件なしで全記事が返ってきていた

**解決結果**:
- 正しいパラメータ（`search`）でテストすると正常に動作することを確認
- マッチしない検索では空の結果 (`data: []`) が正しく返される
- フロントエンドでは既に「記事が見つかりませんでした」の表示が実装済み

**テスト結果**:
- 空文字検索: 正常動作（全記事表示）
- 特殊文字検索: 正常動作（空の結果）
- 他のフィルタとの併用: 正常動作
- ページネーションとの組み合わせ: 正常動作

**実装したコードの場所・ファイル名**:
- `/api/internal/application/usecase/article_usecase_test.go` - 記事関連の単体テスト
- `/api/doc/article_api.md` - APIドキュメント

**学習・知見**:
- テスト時にAPIの正確なパラメータ名を確認することの重要性
- 既存の実装状況を正確に把握してから作業することの大切さ
- チケット管理でのDONE条件の確認と状態更新の重要性

**次回の予定**:
- チケット #2025-06-22_14（画像アップロード機能）の継続実装
- 残りのフロントエンド機能の実装

**愚痴・雑感**:
- 思ったより多くの機能が既に実装されていて驚いた
- APIテストで間違ったパラメータ名を使ってしまい、無駄に時間を使ってしまった
- チケット管理をきちんとやることで、進捗が可視化されて良い

## 2025-06-23 19:15

**対応チケット**: `#2025-06-23_1`  
**作業内容**:

### 問題の発見と解決
記事一覧でサムネイル画像が表示されない問題を徹底的に調査し、根本原因を特定・修正しました。

### 主要な問題
1. **PostgreSQLリポジトリのSELECTクエリ**: `featured_image`と`thumbnail_image`フィールドがSELECTクエリに含まれていない
2. **Scanメソッド**: 画像フィールドがScan処理に含まれていない
3. **CREATE/UPDATEクエリ**: 画像フィールドが含まれていない
4. **UseCaseの入力構造体**: `ThumbnailImage`フィールドが不足
5. **DTOリクエスト構造体**: `ThumbnailImage`フィールドが不足

### 修正内容

#### 1. PostgreSQLリポジトリ修正
- **ファイル**: `internal/infrastructure/persistence/postgres_article_repository.go`
- **内容**:
  - `FindByID`と`List`のSELECTクエリに`featured_image`, `thumbnail_image`を追加
  - 対応するScan処理に画像フィールドを追加
  - CREATE/UPDATEクエリに画像フィールドを追加

#### 2. UseCaseレイヤー修正
- **ファイル**: 
  - `internal/application/usecase/create_article_usecase.go`
  - `internal/application/usecase/update_article_usecase.go`
- **内容**:
  - `CreateArticleInput`に`ThumbnailImage`フィールドを追加
  - `UpdateArticleInput`に`FeaturedImage`と`ThumbnailImage`フィールドを追加
  - エンティティ作成/更新時の画像フィールド設定を追加

#### 3. DTOレイヤー修正
- **ファイル**: `internal/interfaces/dto/article_dto.go`
- **内容**:
  - `CreateArticleRequest`に`ThumbnailImage`フィールドを追加
  - `UpdateArticleRequest`に`ThumbnailImage`フィールドを追加

#### 4. ハンドラーレイヤー修正
- **ファイル**: `internal/interfaces/handler/article_handler.go`
- **内容**:
  - `convertArticleToResponse`に画像フィールドを追加
  - `convertArticleToListResponse`に`ThumbnailImage`フィールドを追加
  - 記事作成/更新時のDTO→UseCaseマッピングに画像フィールドを追加

### テスト結果
- ✅ **APIレスポンス確認**: `curl "http://localhost:8080/api/v1/articles?page=1&limit=1"`
- ✅ **thumbnail_image返却**: 記事一覧APIで正常に`thumbnail_image`が返される
- ✅ **フィールド整合性**: 全レイヤーで画像フィールドが正しく連携

### 次回の予定（さらなる改善点）
- 実際の画像ファイル配信機能のテスト
- フロントエンドでの画像表示確認
- 画像ファイルのパス整合性確認

### 学習・知見
- **レイヤードアーキテクチャ**: 各レイヤー間のデータ構造の整合性が重要
- **PostgreSQL**: SELECTクエリとScan処理の対応関係
- **Go開発**: 構造体フィールドの追加時は全関連箇所の修正が必要

### 愚痴・雑感
最初はフロントエンドの問題かと思ったけど、実際はバックエンドのデータベースレイヤーでフィールドが取得されていないという根本的な問題だった。レイヤードアーキテクチャだと影響範囲が広くて修正箇所が多いけど、きちんと分離されているので問題の特定はしやすかった。API設計の重要性を再認識。